language: python

python:
  "3.6"

install:
  - pip install -r requirements.txt
  - pip install coverage

script: true
  - nosetests --with-coverage --cover-package=app

after_scripty:
  - coveralls    
